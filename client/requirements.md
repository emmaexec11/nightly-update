## Packages
recharts | Data visualization for success rates
date-fns | Date manipulation and formatting
framer-motion | Smooth animations for habit toggles and transitions
react-day-picker | Calendar components for habit history

## Notes
- Dark mode is the default and primary aesthetic.
- Dashboard API provides aggregated data; hooks need to handle the specific response structure.
- Success rate calculation logic is often handled on backend, but frontend visualizes it (Green >= 80%, Red < 80%).
